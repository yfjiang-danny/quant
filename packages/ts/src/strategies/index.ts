import { StockModel } from "../models/type";

namespace Strategy {
  export function filterMaxRiseDay(
    stocks: StockModel[],
    day = 3
  ): StockModel[] {
    const res: StockModel[] = [];

    stocks.forEach((s) => {});

    return res;
  }
}
